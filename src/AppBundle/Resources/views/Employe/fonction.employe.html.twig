{% extends "base.html.twig" %}
{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
{% block title %}Fonction employe{% endblock %}
{% block bodysaisie %}
{#{% include "@App/messages.html.twig" %}#}
<div class="panel panel-default">
    <div class="panel-heading">
        <h4> Fonction employe <small></small> </h4>
    </div>
    <div class="panel-body">
    {{ form_start(form) }}
    {{ form_widget(form) }}
       <p>
          <button class="btn btn-primary" type="submit"><i class="glyphicon glyphicon-edit"></i> Modifier</button>
       </p>
    {{ form_end(form) }}
    </div>
    <div class="panel-footer">
        <div class="btn-group record_actions">
            <a class="btn btn-primary" href="{{ path('employe_show',{id: user | Mycrypt}) }}">
                <i class="glyphicon glyphicon-list"></i> Retour
            </a>
        </div>
    </div>
</div>
{% endblock %}
{% block js %}
<script>
</script>
{% endblock %}